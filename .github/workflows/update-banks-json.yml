name: Update banks.json

on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"

permissions:
  contents: write

defaults:
  run:
    working-directory: ./packages/data

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v6
        with:
          node-version: 24
      - run: npm ci
      - run: npm run update
        env:
          DATA_WERO_P2P_API_URL: ${{ vars.DATA_WERO_P2P_API_URL }}
          DATA_WERO_ECOM_API_URL: ${{ vars.DATA_WERO_ECOM_API_URL }}
          DATA_GITHUB_ORG: ${{ github.repository_owner }}
          DATA_GITHUB_REPO: ${{ github.event.repository.name }}
          DATA_GITHUB_REF: ${{ github.ref }}
          DATA_ASSET_PATH: packages/data/assets
      - name: Commit bundled data
        run: |
          git config --global user.name 'GitHub Actions Bot'
          git config --global user.email 'sharknoon@users.noreply.github.com'
          git diff --quiet && git diff --staged --quiet || (git commit -am "Automated data update" && git push)
